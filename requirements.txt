# Server deps (DGX Spark: aarch64 / GB10 / CUDA 13). Versions verified working.
# Install torch/torchvision from the cu130 index — see scripts/setup_env.sh.
# torch==2.12.1+cu130        (https://download.pytorch.org/whl/cu130)
# torchvision==0.27.1+cu130
transformers==5.12.1
accelerate==1.14.0
safetensors==0.8.0
huggingface_hub>=1.0
fastapi==0.138.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32
numpy>=1.26
pillow>=10
opencv-python-headless==4.13.0.92
scipy==1.18.0
requests==2.34.2
# open3d            # OPTIONAL, unavailable on aarch64 — analytic grasp uses scipy fallback
# --- Contact-GraspNet backend extras (only if using grasp_backend=cgn) -------
# trimesh==4.12.2   # required by contact_graspnet_pytorch at import
# pyrender==0.1.45  # imported by its data module (set PYOPENGL_PLATFORM=egl, headless)
